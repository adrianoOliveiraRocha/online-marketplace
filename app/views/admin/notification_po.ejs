<%if(quantity > 0) {%>
<a href="#" class="dropdown-toggle" data-toggle="dropdown">
  <i class="fa fa-bell-o"></i>
  <span 
    class="label label-warning">

    <%=quantity%>
    
  </span>
</a>
<ul class="dropdown-menu">

  <li class="header">Você tem <%=quantity%> novo(s) pedido(s)</li>

  <li>
    <!-- inner menu: contains the actual data -->
    <ul class="menu">
      <% for(let order of orders) { %>
      <li>
        <a href="/admin_order_details?orderId=<%=order.id%>">
          <i class="fa fa-shopping-cart text-green"></i>           
            <%=fixDate(order.orderDate)%> <%=fixHour(order.orderDate)%>          
        </a>
      </li>
      <% } %>
      
    </ul>
  </li>
  <li class="footer"><a href="/admin_pending_orders">Ver todos</a></li>
</ul>
<% } %>